@model IEnumerable<ObjetosIngresos.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Documento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ficha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirebaseUid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSedePrincipalNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Documento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ficha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirebaseUid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSedePrincipalNavigation.IdSede)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoUsuarioNavigation.IdTipoUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUsuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdUsuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUsuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
